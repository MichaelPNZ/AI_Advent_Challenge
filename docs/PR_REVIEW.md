# 🤖 Автоматическое ревью Pull Request с AI и RAG

Этот проект включает систему автоматического ревью кода с использованием:
- **AI (OpenAI GPT-4)** для анализа изменений
- **RAG (Retrieval Augmented Generation)** для контекста из документации и кода
- **MCP (Model Context Protocol)** для получения git diff через Git Task Tool Client

## 🎯 Возможности

### Что анализирует система

1. **Краткая сводка изменений** - понятное описание что было сделано
2. **Потенциальные проблемы и баги** - выявление возможных ошибок
3. **Нарушения архитектуры** - несоответствия паттернам проекта
4. **Проблемы безопасности** - уязвимости и небезопасные практики
5. **Советы по улучшению** - рекомендации по рефакторингу

### RAG интеграция

При включенном RAG система:
- Ищет релевантные части документации (README, PROJECT.md)
- Находит связанный код из индекса embeddings
- Использует этот контекст для более точного анализа
- Ссылается на источники в ревью

## 🚀 Использование

### Автоматическое ревью через GitHub Actions

1. **Настройка секретов**

   В настройках репозитория добавьте секрет:
   - `OPENAI_API_KEY` - ваш API ключ OpenAI

2. **Триггеры**

   Workflow автоматически запускается при:
   - Открытии нового PR
   - Обновлении PR (новые коммиты)
   - Повторном открытии PR

3. **Результат**

   Бот автоматически добавит комментарий к PR с результатами анализа.

### Локальное использование

#### Быстрое ревью (без RAG)

```bash
export OPENAI_API_KEY="your-api-key"

./gradlew :desktopApp:runPrReview \
  -Pbase=origin/main \
  -PuseRag=false \
  -PoutputFormat=markdown
```

#### Полное ревью с RAG

1. Убедитесь что Ollama запущен и модель скачана:
   ```bash
   ollama serve
   ollama pull nomic-embed-text
   ```

2. Запустите ревью:
   ```bash
   export OPENAI_API_KEY="your-api-key"

   ./gradlew :desktopApp:runPrReview \
     -Pbase=origin/main \
     -PuseRag=true \
     -PminScore=0.25 \
     -PoutputFormat=markdown
   ```

#### Параметры

- `base` - базовая ветка для сравнения (по умолчанию `origin/main`)
- `useRag` - использовать RAG (по умолчанию `true`)
- `model` - ID модели OpenAI (по умолчанию `gpt-4o`)
- `minScore` - минимальный порог релевантности для RAG (по умолчанию `0.25`)
- `outputFormat` - формат вывода: `text`, `markdown`, `json` (по умолчанию `markdown`)

#### Переменные окружения

Альтернативно можно использовать переменные окружения:

```bash
export OPENAI_API_KEY="your-api-key"
export PR_REVIEW_BASE=origin/develop
export PR_REVIEW_USE_RAG=true
export PR_REVIEW_MODEL=gpt-4o
export PR_REVIEW_MIN_SCORE=0.30
export PR_REVIEW_OUTPUT_FORMAT=json

./gradlew :desktopApp:runPrReview
```

## 📊 Примеры вывода

### Markdown формат

```markdown
# 🤖 AI Code Review

## 📝 Результаты анализа

### Краткая сводка
Добавлена новая функциональность для автоматического ревью PR...

### Потенциальные проблемы
- В файле `PrReviewService.kt:45` отсутствует обработка null...

### Советы по улучшению
- Рассмотрите кэширование результатов embeddings...

## 📁 Изменённые файлы
- `desktopApp/.../review/PrReviewService.kt`
- `.github/workflows/pr-review.yml`

## 📚 Использованный контекст (RAG)
- `README.md`
- `docs/PROJECT.md`

## 📊 Метрики
| Параметр | Значение |
|----------|----------|
| Модель | gpt-4o |
| Время | 12500ms |
| Токены | 4521 |
| RAG чанки | 5 |
| Уверенность | 87% |
```

### JSON формат

```json
{
  "summary": "Полный текст ревью...",
  "confidence": 0.87,
  "changedFiles": ["file1.kt", "file2.kt"],
  "contextFiles": ["README.md", "docs/PROJECT.md"],
  "metrics": {
    "modelId": "gpt-4o",
    "durationMillis": 12500,
    "tokensUsed": 4521,
    "chunksUsed": 5
  }
}
```

## 🏗️ Архитектура

### Компоненты

1. **PrReviewService** (`desktopApp/.../review/PrReviewService.kt`)
   - Основной сервис для анализа PR
   - Интеграция с GitTaskToolClient для получения diff
   - Использование RAG для контекста
   - Генерация структурированного ревью

2. **PrReviewRunner** (`desktopApp/.../review/PrReviewRunner.kt`)
   - CLI интерфейс для запуска ревью
   - Парсинг параметров командной строки
   - Форматирование вывода

3. **GitTaskToolClient** (`core/network/.../GitTaskToolClient.kt`)
   - MCP инструмент для работы с git
   - Функции: `git_branch`, `git_diff`, `git_show_file`

4. **GitHub Actions** (`.github/workflows/pr-review.yml`)
   - Автоматизация процесса ревью
   - Публикация результатов в комментарии

### Workflow

```
PR создан/обновлён
    ↓
GitHub Action запускается
    ↓
Установка Ollama + nomic-embed-text
    ↓
Сборка проекта
    ↓
Индексация документации (RAG)
    ↓
Получение diff через GitTaskToolClient
    ↓
Поиск релевантного контекста через RAG
    ↓
Анализ с помощью GPT-4
    ↓
Публикация результатов в PR
```

## 🔧 Настройка и расширение

### Добавление своих проверок

Расширьте `PrReviewService.kt`:

```kotlin
private fun buildReviewPrompt(...): String = buildString {
    // ... существующий код ...
    appendLine("6. **Мои кастомные проверки** - дополнительные правила")
}
```

### Изменение модели

В `PrReviewRunner.kt` или через параметры:

```bash
./gradlew :desktopApp:runPrReview \
  -Pmodel=gpt-4o-mini  # Или другая модель OpenAI
```

### Настройка RAG

Измените порог релевантности:

```bash
./gradlew :desktopApp:runPrReview \
  -PminScore=0.35  # Более строгая фильтрация
```

### Добавление индексирования

Для предварительной индексации документации:

```kotlin
// В GitHub Actions перед review
val indexService = EmbeddingIndexService(...)
indexService.buildIndex(File("docs"))
indexService.buildIndex(File("README.md"))
```

## 🐛 Отладка

### Просмотр логов локально

```bash
./gradlew :desktopApp:runPrReview -PoutputFormat=text
```

### Проверка Ollama

```bash
ollama list  # Проверить установленные модели
ollama serve  # Запустить сервер
curl http://localhost:11434/api/tags  # API проверка
```

### GitHub Actions логи

1. Перейдите в раздел "Actions" вашего репозитория
2. Выберите запуск "AI PR Review"
3. Посмотрите детальные логи каждого шага

### Артефакты

После каждого запуска сохраняются:
- `pr-review-report.md` - результат ревью
- `ollama.log` - логи Ollama (если есть проблемы с embeddings)

## ⚠️ Известные ограничения

1. **Размер diff**: Очень большие PR (>10k строк) могут превышать контекст модели
2. **Время выполнения**: RAG режим занимает 20-60 секунд в зависимости от размера PR
3. **Ollama в CI**: Установка занимает ~2 минуты, возможно кэширование
4. **API лимиты**: Убедитесь что у вас достаточный лимит OpenAI API

## 💡 Рекомендации

### Для лучших результатов

1. **Поддерживайте документацию актуальной** - RAG работает лучше с хорошей документацией
2. **Индексируйте примеры кода** - добавьте в индекс образцы правильной архитектуры
3. **Настройте порог релевантности** - экспериментируйте с `minScore`
4. **Используйте описательные commit messages** - помогает контексту

### Оптимизация

1. **Кэшируйте embeddings** - переиспользуйте индекс между запусками
2. **Предварительная индексация** - создавайте индекс при изменении документации
3. **Ограничьте размер контекста** - уменьшите `topK` для очень больших PR

## 📚 Дополнительные материалы

- [CLAUDE.md](../CLAUDE.md) - общая документация проекта
- [PROJECT.md](./PROJECT.md) - описание архитектуры
- [Model Context Protocol](https://modelcontextprotocol.io/) - о MCP
- [OpenAI API](https://platform.openai.com/docs/api-reference) - документация OpenAI API
